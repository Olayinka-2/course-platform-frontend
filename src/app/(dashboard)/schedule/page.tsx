import React from 'react'
import Header from '@/layouts/header'

const Schedule = () => {
  const headerItem = {
    title:'Schedule',
    description: "",
  }
  return (
    <div>
      <Header {...headerItem} />
    </div>
  )
}

export default Schedule
